server:
  port: 9000
spring:
  application:
    name: ant-mall
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ant-mall?useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 8816622

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    # SQL日志打印配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 枚举类型处理器
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
    # 显示实际参数（配合log-impl使用）
    log-sql-parameter: true
    # 其他常用配置
    map-underscore-to-camel-case: true  # 自动驼峰转换
    cache-enabled: false                # 关闭二级缓存
    local-cache-scope: statement        # 缓存作用域